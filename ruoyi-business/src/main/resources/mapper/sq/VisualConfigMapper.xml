<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.sq.mapper.VisualConfigMapper">
    <resultMap id="VisualConfigResult" type="com.ruoyi.sq.domain.VisualConfig">
        <result property="id" column="id" />
        <result property="visualId" column="visual_id" />
        <result property="detail" column="detail" />
        <result property="component" column="component" />
    </resultMap>

    <sql id="selectVisualConfigVo">
        select `id`, `visual_id`, `detail`, `component` from `blade_visual_config`
    </sql>

    <select id="selectVisualConfigById" resultMap="VisualConfigResult">
        <include refid="selectVisualConfigVo"/> where `id` = #{id}
    </select>

    <select id="selectVisualConfigList" resultMap="VisualConfigResult">
        <include refid="selectVisualConfigVo"/>
        <where>
            <if test="id != null"> and `id` = #{id}</if>
            <if test="visualId != null"> and `visual_id` = #{visualId}</if>
            <if test="detail != null and detail != ''"> and `detail` = #{detail}</if>
            <if test="component != null and component != ''"> and `component` = #{component}</if>
        </where>
    </select>

    <insert id="insertVisualConfig" parameterType="com.ruoyi.sq.domain.VisualConfig" useGeneratedKeys="true" keyProperty="id">
        insert into `blade_visual_config`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">`id`,</if>
            <if test="visualId != null">`visual_id`,</if>
            <if test="detail != null">`detail`,</if>
            <if test="component != null">`component`,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="visualId != null">#{visualId},</if>
            <if test="detail != null">#{detail},</if>
            <if test="component != null">#{component},</if>
        </trim>
    </insert>

    <update id="updateVisualConfig" parameterType="com.ruoyi.sq.domain.VisualConfig">
        update `blade_visual_config`
        <set>
            <if test="visualId != null">`visual_id` = #{visualId},</if>
            <if test="detail != null">`detail` = #{detail},</if>
            <if test="component != null">`component` = #{component},</if>
        </set>
        where `id` = #{id}
    </update>
    <update id="deleteVisualConfigByIds">
        update `blade_visual_config` set `is_deleted` = '1' where `id` in
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </update>

    <update id="deleteVisualConfigById">
        update `blade_visual_config` set `is_deleted` = '1' where `id` = #{id}
    </update>

</mapper>
