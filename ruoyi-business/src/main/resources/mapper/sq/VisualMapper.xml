<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.sq.mapper.VisualMapper">
    <resultMap id="VisualResult" type="com.ruoyi.sq.domain.Visual">
        <result property="id" column="id" />
        <result property="title" column="title" />
        <result property="backgroundUrl" column="background_url" />
        <result property="category" column="category" />
        <result property="password" column="password" />
    </resultMap>

    <sql id="selectVisualVo">
        select `id`, `title`, `background_url`, `category`, `password` from `blade_visual`
    </sql>

    <select id="selectVisualById" resultMap="VisualResult">
        <include refid="selectVisualVo"/> where `id` = #{id}
    </select>

    <select id="selectVisualList" resultMap="VisualResult">
        <include refid="selectVisualVo"/>
        <where>
            <if test="id != null"> and `id` = #{id}</if>
            <if test="title != null and title != ''"> and `title` = #{title}</if>
            <if test="backgroundUrl != null and backgroundUrl != ''"> and `background_url` = #{backgroundUrl}</if>
            <if test="category != null"> and `category` = #{category}</if>
            <if test="password != null and password != ''"> and `password` = #{password}</if>
        </where>
    </select>

    <insert id="insertVisual" parameterType="com.ruoyi.sq.domain.Visual" useGeneratedKeys="true" keyProperty="id">
        insert into `blade_visual`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">`id`,</if>
            <if test="title != null">`title`,</if>
            <if test="backgroundUrl != null">`background_url`,</if>
            <if test="category != null">`category`,</if>
            <if test="password != null">`password`,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="title != null">#{title},</if>
            <if test="backgroundUrl != null">#{backgroundUrl},</if>
            <if test="category != null">#{category},</if>
            <if test="password != null">#{password},</if>
        </trim>
    </insert>

    <update id="updateVisual" parameterType="com.ruoyi.sq.domain.Visual">
        update `blade_visual`
        <set>
            <if test="title != null">`title` = #{title},</if>
            <if test="backgroundUrl != null">`background_url` = #{backgroundUrl},</if>
            <if test="category != null">`category` = #{category},</if>
            <if test="password != null">`password` = #{password},</if>
        </set>
        where `id` = #{id}
    </update>
    <update id="deleteVisualByIds">
        update `blade_visual` set `is_deleted` = '1' where `id` in
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </update>

    <update id="deleteVisualById">
        update `blade_visual` set `is_deleted` = '1' where `id` = #{id}
    </update>

</mapper>
